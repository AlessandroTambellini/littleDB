{
    "tasks": [
        {
            "label": "build active file",
            "type": "shell",
            "command": "gcc",
            "args": [
                "-g", "-Wall", "-Wextra", "${file}", "-o", "${workspaceFolder}/${fileBasenameNoExtension}.out"
            ],
            "problemMatcher": ["$gcc"],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Generated task for building the C program"
        }
    ],
    "version": "2.0.0",
}


